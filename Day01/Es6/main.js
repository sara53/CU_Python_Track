export function add(x, y) {
  // 100 line
  return x + y;
}

export function square() {
  // 40 line
  return add(3, 3) * 2;
}

export default function sayHello() {
  console.log("Welcome Python track");
}

export let fname = "ali";

/** (export)
 * export Naming
 * export default
 *
 */
